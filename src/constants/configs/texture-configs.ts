import { textures } from '../../assets/textures';

export const getHandTextureConfig = (): TextureConfig => {
    return textures['ui/hand'];
};

export const getLogoTextureConfig = (): TextureConfig => {
    return textures['ui/logo'];
};

export const getSoundOnTextureConfig = (): TextureConfig => {
    return textures['ui/sound_on'];
};

export const getSoundOffTextureConfig = (): TextureConfig => {
    return textures['ui/sound_off'];
};
